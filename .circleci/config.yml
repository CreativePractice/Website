orbs:
  react: 'thefrontside/react@dev:alpha'
version: 2.1
workflows:
  build:
    jobs:
      - steps:
        - run: cd ./my-app/
      - react/install
      - react/test:
          requires:
            - react/install
